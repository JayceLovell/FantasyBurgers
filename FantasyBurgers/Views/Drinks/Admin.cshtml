@model IEnumerable<FantasyBurgers.Models.Drink>

@{
    ViewBag.Title = "Drinks Admin";
}

<h2>Drinks Admin</h2>

<p>
    @Html.ActionLink("Create New Drink", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Drink1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DrinkShortDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DrinkPrice)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Drink1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DrinkShortDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DrinkPrice)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DrinkId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DrinkId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DrinkId })
        </td>
    </tr>
}

</table>
